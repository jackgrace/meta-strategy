[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Web service — daily cron handled internally (midnight AEST)
# Manual triggers via /run or Slack slash command
healthcheckPath = "/health"
healthcheckTimeout = 10
